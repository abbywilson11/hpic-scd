// frontend/src/apiConfig.js
export const API_BASE_URL = // Base URL for API for rendering in render.com
  import.meta.env.VITE_API_BASE_URL || "http://localhost:5000"; // Default to localhost if not set
